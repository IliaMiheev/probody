# В данной папке хранятся шаблоны кода для автономного полёта:

## 1. emergency_and_navigate.py

### Указаны все необходимые импорты, также функция перехвата Ctrl+C (signal_handler), безопасной посадки (emergency_stop), navigate_wait

## 2. color.py

### Функция определения цвета платформы. Также показано, как вызывать данную функцию (через подписчика (Subscriber))

## 3. qr.py

### Функция считывания и расшифровки qr кода. Также показано, как вызывать данную функцию (через подписчика (Subscriber))
